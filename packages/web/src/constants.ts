/**
 * Application-wide constants
 */

// Toast configuration
export const TOAST_DURATION = 4000; // milliseconds

// Layout configuration
export const DEFAULT_PREVIEW_RATIO = 0.4; // 40% for preview
export const SIDEBAR_MIN_WIDTH = 180;
export const SIDEBAR_MAX_WIDTH = 400;
export const DEFAULT_SIDEBAR_WIDTH = 260;

// Recent files limit
export const MAX_RECENT_FILES = 10;

// Editor configuration
export const EDITOR_DEBOUNCE_DELAY_MS = 300; // Debounce delay for content updates

// Sync configuration
export const SYNC_DEBOUNCE_DELAY_MS = 1000; // Debounce delay for sync operations

// Timeout configuration
export const MONACO_INIT_TIMEOUT_MS = 30000; // Monaco editor initialization timeout
export const API_REQUEST_TIMEOUT_MS = 10000; // API request timeout

// E2E test configuration
export const E2E_WAIT_TIMEOUT_MS = 5000; // Default wait timeout for E2E tests

